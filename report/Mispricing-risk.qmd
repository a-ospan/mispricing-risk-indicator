---
title: "Mispricing Risk Indicator"
author: Artykzhan Ospan
format:
  html:
    theme: metropolis
    section-titles: true
    fig-width: 3
    fig-height: 2
    fontsize: 10pt
    self-contained: true   
---

## Introduction

This project builds a Mispricing Risk Indicator using U.S. financial data from 2015 to 2024. The indicator is inspired by the IMF Working Paper **"Predicting Financial Crises: The Role of Asset Prices" (WP/23/157)** and is designed to monitor periods when asset prices may move away from underlying economic fundamentals.

The model combines six signals across equity markets, market volatility, foreign exchange, fixed income, sovereign spread conditions, and housing prices. Each component is transformed into a comparable percentile-based measure and combined into a **0 to 100 composite risk score**, where higher values indicate greater relative mispricing risk.

---

## Indicator Components

The indicator consists of the following six components:

1. **Real Equity Return** – One-year change in the S&P 500, adjusted for inflation
2. **Equity Market Volatility** – Rolling standard deviation of monthly equity returns
3. **FX Volatility** – Volatility in the USD/EUR exchange rate
4. **Bond Yield Volatility** – Changes in 10-year U.S. Treasury yields
5. **Sovereign FX Spread** – Ratio of emerging market bond dividends to their prices
6. **Real House Price Growth** – One-year change in U.S. house prices, adjusted for inflation

Each period shows a different factor affecting pricing or stability, helping to indicate potential overvaluation.

---

## Methodology and Plot

The project uses a composite-indicator approach. Each data series is cleaned, converted to monthly frequency, and transformed into a risk-related measure. Since the variables have different units, each component is converted into a percentile score.
The final Mispricing Risk Indicator is calculated as the average of the six component scores. The index ranges from **0 to 100**, where higher values indicate higher relative mispricing risk.

Risk zones are defined as follows:

| Score Range | Risk Level |
|---|---|
| 0-40 | Low |
| 40-66.7 | Moderate |
| 66.7-80 | Early Warning |
| 80-100 | High |

---

## Methodology and Plot

```{python}
import pandas as pd, numpy as np, matplotlib.pyplot as plt

load = lambda f: pd.read_csv(f"data/{f}.csv", names=["DATE", f], header=0, parse_dates=["DATE"]).dropna().set_index("DATE").apply(pd.to_numeric, errors="coerce")
files = ["embDiv", "embPrice", "GS10", "DEXUSEU", "CPI", "SP500", "USSTHPI"]
data = {f: load(f) for f in files}; data["USSTHPI"].columns = ["house_price"]

df = pd.concat(data.values(), axis=1).resample("QE-DEC").mean(numeric_only=True)
pct = lambda s, n=1: s.pct_change(n, fill_method=None)
comp = pd.DataFrame(index=df.index)
comp["real_equity_return"] = pct(df["SP500"],4) - pct(df["CPI"],4)
comp["equity_vol"] = pct(df["SP500"]).rolling(3).std()
comp["fx_vol"] = pct(df["DEXUSEU"]).rolling(3).std()
comp["bond_yield_vol"] = pct(df["GS10"]).rolling(3).std()
comp["fx_spread"] = df["embDiv"] / df["embPrice"]
comp["real_house_price_growth"] = pct(df["house_price"],4) - pct(df["CPI"],4)

p = comp.dropna().rank(pct=True) * 100
for c in ["equity_vol","fx_vol","bond_yield_vol","fx_spread"]: p[c] = 100 - p[c]
p["Mispricing_Risk"] = p.mean(1); r = p["2015":"2024"]

r["Mispricing_Risk"].plot(figsize=(12,6), lw=2, color="blue", title="U.S. Mispricing Risk")
plt.axhline(66.7, color="orange", ls="--", lw=1.5, label="66.7 Early Warning")
plt.axhline(80, color="red", ls="--", lw=1.5, label="80 High Risk")
plt.ylabel("Percentile"); plt.grid(); plt.legend(); plt.tight_layout(); plt.show()
```

---

## Key Drivers by Period

| Period | Main Drivers | Interpretation |
|---|---|---|
| 2016 to 2019 | Equity volatility and bond yield volatility | The indicator stayed mostly stable, while short term increases were linked to market uncertainty and changes in interest rate expectations. |
| 2018 Spike | Equity volatility and bond yield volatility | Risk increased as markets reacted to tighter monetary policy, higher Treasury yield movements, and trade related uncertainty. |
| 2020 to 2021 Peak | Real equity return, real house price growth, and low rate conditions | The risk score rose as equity prices and housing prices recovered quickly during a period of low interest rates and policy support. |
| 2022 Decline | Lower equity momentum and tighter bond market conditions | The indicator declined as the Federal Reserve raised rates, financial conditions tightened, and asset prices adjusted. |
| 2023 to 2024 Rebound | Equity recovery and housing resilience | Risk began to rise again as market confidence improved, equity prices recovered, and housing market conditions remained relatively strong. |

---

## Conclusion

The U.S. Mispricing Risk Indicator looks for signs of overvaluation in financial assets by combining market and macroeconomic data. It has continuously coincided with times of financial and economic instability during the last ten years. The indicator offers a simple and useful method for evaluating changes in market sentiment and spotting pricing that departs from core values.

---

## Reference

International Monetary Fund (2023). *Predicting Financial Crises: The Role of Asset Prices*. IMF Working Paper WP/23/157.
